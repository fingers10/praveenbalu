{
  "scripts": {
    "dev": "tailwindcss -i style.css -o app.css --watch",
    "build": "tailwindcss -i style.css -o app.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.13",
    "tailwindcss": "^4.1.13"
  }
}